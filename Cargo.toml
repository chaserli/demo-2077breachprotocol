[package]
name = "rust_breach_protocol"
version = "0.0.0"
edition = "2024"

[dependencies]
rand = "0.8"
macroquad = { version = "0.4", features = ["audio"] }

[package.metadata.bundle]
name = "Breach Protocol"
icon = ["assets/icons/logo.icns", "assets/icons/logo.ico"]
